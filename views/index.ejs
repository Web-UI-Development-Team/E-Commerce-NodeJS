<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Books</title>
</head>
<body>
    <h1><%=email%></h1>
    <ul>
        <% for (var i = 0; i < books.books.length; i++) { %>
        <li>
            <div>
                <span class="date" style="text-align:left">
                <%= books.books[i].title %></span>
                <span class="time" style="font-weight:700;font-size:25px;line-height:20px;text-align:left;">
                <%= books.books[i].author %> </span>
            </div>
        </li>
        <% } %>
    </ul>
</body>
</html>